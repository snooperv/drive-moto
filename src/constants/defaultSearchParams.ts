import { PageSize, PageSizeFavourites } from "./pageSetting";

export const defaultSearchParams = {
  PageNumber: "1",
  PageSize: String(PageSize),
};

export const defaultParamsFavourite = {
  PageNumber: "1",
  PageSize: String(PageSizeFavourites),
};
