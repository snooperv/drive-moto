import React from "react";
import styles from "./filters.module.scss";

const NotFilter = () => {
  return <div className={styles.container}>Фильтр находится в разработке</div>;
};

export default NotFilter;
